# Example environment variables for the client
# Copy this to .env (or .env.local) and fill values for local development

# Backend API URL used by the client (optional, defaults to http://localhost:5000)
VITE_API_BASE_URL=http://localhost:5000

# Google OAuth client id (optional). If you don't use Google OAuth, leave blank.
# To enable Google sign-in, create an OAuth 2.0 Client ID in Google Cloud Console and set the value below.
VITE_GOOGLE_CLIENT_ID=

# EmailJS (optional) - used for welcome/reset emails in the demo
VITE_EMAILJS_SERVICE_ID=
VITE_EMAILJS_TEMPLATE_ID=
VITE_EMAILJS_PUBLIC_KEY=

# Any other VITE_ variables used by the client can be added here
